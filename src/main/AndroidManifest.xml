<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nao">
        <activity android:name=".message.ChatActivity"></activity>
        <activity android:name=".message.MessagesActivity" />
        <activity android:name=".other.UserProfile" />
        <activity android:name=".other.FindPartner" />
        <activity android:name=".NewUsersActivity" />
        <activity android:name=".NewUsersInfoActivity" />
        <activity android:name=".ExistingUsersActivity" />
        <activity android:name=".TermsOfServiceActivity" />
        <activity android:name=".FirstPageProfileInfoActivity" />
        <activity android:name=".SecondPageProfileInfoActivity" />
        <activity android:name=".other.BioActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>